name: Check the scripts

# Runs PSScriptAnalyzer on every push, so mistakes show up here
# and not on someone's server.
# The rules it uses are in PSScriptAnalyzerSettings.psd1, which explains
# why one rule is turned off.

on:
  push:
    branches: [main]
  pull_request:

jobs:
  analyze:
    runs-on: windows-latest

    steps:
      - name: Get the code
        uses: actions/checkout@v4

      - name: Install PSScriptAnalyzer
        shell: pwsh
        run: Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser

      - name: Run the check
        shell: pwsh
        run: |
          $found = Invoke-ScriptAnalyzer -Path . -Recurse -Settings ./PSScriptAnalyzerSettings.psd1
          if ($found) {
            $found | Format-Table -AutoSize
            Write-Error "PSScriptAnalyzer found $($found.Count) problem(s)."
            exit 1
          }
          Write-Host "No problems found."
